info:
  name: auth
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8000/api/auth
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
