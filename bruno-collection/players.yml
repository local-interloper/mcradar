info:
  name: players
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8000/api/players
  body:
    type: json
    data: |-
      {
        "pagination": {
          "first": 0,
          "last": 50
        },
        "filters": {
          "username": "Milano"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
